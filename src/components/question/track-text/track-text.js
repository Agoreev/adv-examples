import React from "react";

const trackText = ({ oldText, newText }) => {
    return (
        <div>
            <p>{oldText}</p>
            <p>{newText}</p>
        </div>
    );
};

export default trackText;
